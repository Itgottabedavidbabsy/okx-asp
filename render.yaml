services:
  - type: web
    name: okx-asp-backend
    runtime: node
    plan: free
    rootDir: backend
    buildCommand: npm install && npx prisma generate
    startCommand: npx prisma db push --accept-data-loss && node src/server.js
    envVars:
      - key: NODE_ENV
        value: production
